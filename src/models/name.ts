export class Name {
    name: string
    frequency: string

    constructor(
        name: string,
        frequency: string
    ) {
        this.name = name
        this.frequency = frequency
    }
}